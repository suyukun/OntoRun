"""存储适配层（重写蓝图 v0.3 §1.3 / 补丁 v0.3.1 B2）。

按补丁 B2：仅保留 **GraphStore** 抽象接口 + **SQLite 关系表实现**（服务 /graph 端点）；
VectorStore / Cache 一律删除（零用例，纯 YAGNI）。

GraphStore 设计原则（蓝图 §1.3 / §10 决策 11）：
- 抽象接口（GraphStore）先于具体实现，业务代码只依赖接口；
- 默认实现 = SQLite 关系表（节点/边两张表），与本体库共库或独立库皆可；
- 外部图库（如 Neo4j）只留替换位不引依赖，将来按需新增实现类。
"""
