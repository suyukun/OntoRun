"""逻辑规则与动作类型子包（重写蓝图 v0.3 §3 / §4 logic_rules+action_types+action_runs）。

职责：
- 逻辑规则真实推导（不硬塞 7 条固定模板，§10 决策 1.1.3）：从 schema 约束/
  状态字段实际推导，覆盖状态机/前置条件/数值阈值等；
- 状态机：E4（补丁 B4 仅 P1 object_types/link_types 走发布；P0 阶段逻辑规则
  与动作走 draft→published，状态字段与蓝图 §4 一致）；
- 动作类型对接：复用现有 runtime 动作引擎（蓝图 §10 决策 4）；
- E6 快照审计：动作执行记录 before/after 快照（action_runs 表），并入现有审计。

注意补丁 A2：动态对象类型的写回列发布期 TODO；本子包只注册动作元数据 +
E6 审计，不执行动态写回。

P0 仅子包骨架；P4 实现规则推导 + 动作对接 + 快照审计。
"""
