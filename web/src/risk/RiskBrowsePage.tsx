// /risk/browse 薄壳 —— 仅渲染数据工作台（DataExplorer + 风险对话双栏）；
// 路由 slug（/risk/browse）与既有测试引用保持不变，主题由 RiskShell 统一提供。
import WorkbenchPage from './WorkbenchPage';

export default function RiskBrowsePage() {
  return <WorkbenchPage />;
}
