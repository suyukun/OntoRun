# 财富广场·口径是非题清单 v1（2026-09-10，待数仓同学勾选）

> 用法：Jack 转发给同学，每题勾"对/错/不确定"，错的一句纠正。全部来自 ETL 脚本机器解析+人工核读，出处可追溯。确认后进本体注册表（unverified→confirmed）。

## A. 注册 KPI 口径（出处：脚本dwd_cu_rgst_fin_di.sql）

**Q1** 注册 KPI 明细 = 两段拼：①用户维当日新增且渠道**不含**麦当劳/中信书院（直接注册）；②这两个渠道的用户**激活后**才算（if_act=1）。——对吗？

**Q2** 激活判定的时间边界至今仍是：关联渠道类（actv_tag=1）2024-07-01 起算、此前及"中信优享+公众号"2024-06-30 前不算；企微/小程序关注类（actv_tag=2）2024-07-09 上线起算。——这两个日期现在还有效吗？

**Q3** 一人多条激活记录时，按激活时间取**最早一条**计入。——对吗？

**Q4** 同一用户同日"直接注册"和"激活"都发生时，走直注段（if_act=0），不会双计。——对吗？

## B. 渠道与对账（出处：脚本ads_rgst_chnl_cnt_df.sql + 镜像库实证）

**Q5** 渠道分组一律以**明细渠道编码 JOIN 渠道维表**的结果为准（三级层级），**不以**明细自带的渠道名字段为准。——对吗？（我们在数据里发现过两者不一致的记录，数仓口径落在 JOIN 侧）

**Q6** 明细渠道编码在维表无匹配时，记录落"渠道为空"组（left join），不报错不丢弃。——这是**预期行为**，还是待治理的数据质量问题？现在生产上 null 组大概什么量级？

**Q7** 观远报表的"注册用户数"与 ads_rgst_chnl_cnt_df 的五个时间口径（当日/近7日/当月/当年/历史累计）一一对应，无额外加工。——对吗？

## C. 时间与刷新语义

**Q8** `_di` 结尾=每日增量（只含当日新增行）、`_df` 结尾=每日全量快照（每日整表重建）。——对吗？有无例外？

**Q9** 注册明细里 rgst_dt=加工分区日期、rgst_dt_src=用户原始注册日期，两者可能不同（如补录/跨日）。**KPI 口径以 rgst_dt 为准**。——对吗？什么场景会出现两者不同？

**Q10** dim_date_holiday 每年手工插入（Jack 已确认）。"近 7 日"等口径全部按**自然日**计算，节假日不特殊处理。——对吗？

---
*来源：口径发现卡片（docs/财富广场-口径发现卡片-2026-09-10.md）+ 镜像库对账实证。回答"不确定"的题，Rose 会带着具体数据再问一轮。*
