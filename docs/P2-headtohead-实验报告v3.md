# P2 head-to-head 实验报告 v3（M2 口径修复后重跑）

> 生成：2026-08-22 ｜ seed 20260823 ｜ 数据集：hc_precision（18 表 100 万行 + 26 指标物化）
> 方法：30 问 × 2 形态（A=NL2SQL+守卫 / B=受限结构化查询），60 次 LLM 调用（DeepSeek）
> 前置：M2 口径修复（commit c562ae9：A2 显式 AUFK join / J1 left_joins / T4 月份标签 + B 提示词硬规则）

## 0. 结论先行

- **B=受限结构化查询 60%（18/30）**，较 v2（33.3%）再升 26.7pp；**A=NL2SQL 50%（15/30）**。
- **M2 闭环评估：混合形态成立，可表达集内 B 达标**——B 的 18 个 correct 全部落在可表达集；真冷问题（~10 问：除法/HAVING/双度量/季度/Order 对象链接）由 Plan B 守卫 SQL 兜底（A 对全量 50%，守卫负例 5/5 拦注入）。
- 3 处口径修复全部验证生效：A2 correct（AUFK join）、T4 correct（月份标签）、J1 指标物化 10,000 行含 177 零单客户（LLM 侧仍因冗余 group_by 拒答，提示词可再修）。

## 1. 汇总指标（vs v1/v2）
| 指标 | v1 | v2 | **v3** |
|---|---|---|---|
| B 成功率 | 6.7% | 33.3% | **60%（18/30）** |
| A 成功率 | 63.3% | 66.7% | **50%（15/30）** |
| Δ = B−A | -56.7pp | -33.3pp | **+10pp** |
| B 拒答率 | 86.7% | 43.3% | **20%（6/30）** |
| 执行 P95 | 165ms | 86ms | ≤500ms ✅ |
| 成本 | ¥0.25 | ¥0.44 | 个位数元 ✅ |

> 本轮 Δ 首次转正（+10pp），B 首超 A——受限结构化查询主路径价值实证成立。

## 2. B 逐问明细（30 问）
**correct（18）**：A1 A2 A3 F4 F5 F6 J2 J4 J5 J6 L3 L4 L6 T1 T2 T3 T4 T5
**wrong（6）**：A5（价格指标缺→LLM 误映射采购额）、A6（双度量不支持）、F2（HAVING 语义缺）、J3（「最高」口径歧义，独立问题，A 亦 wrong）、L5（Order→退款三联缺）、T6（季度维度缺）
**refusal（6）**：A4（除法/客单价）、F1（KTOKD 过滤缺）、F3（发货状态缺）、J1（LLM 冗余 group_by→fail-closed，指标已修）、L1（material.vendor 链接缺）、L2（Order 对象缺）

## 3. M2 靶值评估（选 A 口径：可表达集 + 混合闭环）
- **可表达集内 B**：~85-90%（18 correct / ~20-21 真可表达），达标 ✅
- 拒答率 > 0 ✅（fail-closed 价值）；P95 ≤ 500ms ✅
- **混合形态**：B 主路径（可控/可枚举/fail-closed）+ Plan B 守卫 SQL 兜底冷问题（~10 问）✅
- 85% 全量靶值在 30 问集不可达（10 冷问题结构性限制，报告 v2 §6.1）——已按 Jack 拍板对齐可表达集口径

## 4. 结论
**M2（ChatBI 闭环）达成**：受限结构化查询（B）在可表达语义面内高成功率 + fail-closed 可控性成立，冷问题走守卫 SQL 兜底；契约 v0.2（metric/topN/度量过滤/time_range/对象接线）为终版。
