# S2 议题清单 v0.2 —— 玩具 → 生产的跨越

> 版本：v0.13 ｜ 日期：2026-08-20 ｜ 编制：Rose ｜ 状态：前置决策已拍板（2026-08-21），S2 计划定稿
> 性质：内部讨论文档，**不进公开白名单**（publish_to_github.py EXACT_FILES 不含本文档）。
> 起点：S1（MVP 闭环）已收官，S1→S2 迁移判据成立（P6 全链路跑通 + 三问测试通过 + 发布完成）。

## 0.5 讨论管理机制（Jack 确认 2026-08-20）

**核心原则：不按难易，按影响面分层 + 滚动复核。** 简单议题先做的代价是：复杂议题结论一变，前面简单结论返工。故：

1. **议题分三档（按影响面）**：
   - **A 定调档**：#9 企业生成工具、#1 ChatBI、#8 方法论 —— 决定 S2 骨架，**先定"形态"**（一句话说清它是什么），细节后补；
   - **B 支撑档**：#2 规模化治理、#3 权限、#4 会话持久化 —— 依赖 A 的形态才能定；
   - **C 工程档**：#5 真实场景、#6 CI/UX、#7 一键起 —— 独立可收口，随时插空。
2. **A 类内部有先后**：#9（造真实企业战场）→ #1（ChatBI 在战场上演进）→ #8（方法论从落地里长出来）。落地先行，方法论不先空写。
3. **滚动收敛**：每个议题收口时检查依赖它的议题结论要不要改；每轮结束全量复核一次；新议题随时登记、定影响面后**入下一轮**，不插队当前轮。
4. **单一事实来源**：本文档每议题维护四字段 = 状态（待定/形态已定/细化/收口）+ 结论（一句话）+ 依赖 + 待决。结论一变 → 更新清单 → 标记受影响议题复核。

**不造轮子铁律（Jack 强调 2026-08-21，全 S2 适用）**：
- 成熟可用 = 大量人使用 + 充分认可（不因"能用"就当成熟）；
- 技术组件（引擎/框架/协议）一律优先成熟件：分析引擎 DuckDB、指标层 dbt Semantic Layer/LookML、LLM SDK、前端框架等；
- 自研边界 = 仅"研究本身"：本体运行时（S1 已定）、本体↔分析引擎映射（ChatBI 差异化点）。其余任何可被成熟件覆盖的，禁止自研；
- 落地时先调研候选成熟件（含国内可达性），再决定自研 vs 引入，绝不默认自研。

**当前讨论序**：A 档 #9 → #1 → #8（只先定形态）；B/C 档随后。

## 0. S2 的本质：把「机制可行」变成「经得起真实企业的量级、规模与治理」

S1 证明的是"一条端到端流程能通过语义接口闭环走通"；S2 要回答的是三件事：

1. **量级**：几百 TB~PB 级数据下语义接口不崩（ChatBI 是刀刃上最难的场景）；
2. **规模**：对象/动作从几个到几百上千，治理方式不再靠手写注册表；
3. **可信**：从演示环境到有权限边界、有审计、会话不丢、能给别人用的生产要素。

## 1. 议题全景（9 项）

| # | 议题 | 本质 | 依赖/状态 |
|---|---|---|---|
| 1 | **ChatBI/智能分析系统规划** | 量级问题（分析语义层/预聚合/结构化查询） | ✅ 核心价值；**形态已定**（2026-08-21 v2） |
| 2 | **对象/动作规模化治理** | 规模问题（builder 自动识别+注册表+校验规模化） | **形态已定**（2026-08-21）：映射=分级置信度+人审工作台；动作模板库；校验注册表 |
| 3 | **权限/鉴权** | 可信要素（F7-F9：GET runs 鉴权、快照保护、actor 治理） | **形态已定**（2026-08-21）：双主体 + 对象/属性级权限于语义层 |
| 4 | **会话持久化** | 可信要素（TD-15 迁 Redis/DB；前端 localStorage 已做 MVP 部分） | **形态已定**（2026-08-21）：SQLite 单表 + TTL30 天 + 审计关联 |
| 5 | **真实场景试用** | **S2 硬判据**（找真实业务场景给 OntoRun 用） | 贯穿；候选场景待 Jack 提供 |
| 6 | **CI + 前端 UX 打磨** | 生产观感（GitHub Actions workflow 已 active，权限确认后真跑） | 权限确认后 |
| 7 | 一键起服务 start_dev.sh | 顺手小项 | 可选 |
| **8** | **本体论落地实施方法论** | 研究核心：从"机制"到"可复述、可迁移的方法论"（与白皮书 §7 联动） | 🆕 Jack 新增；**形态基本定**（2026-08-21，世界级难题不锁定） |
| **9** | **真实企业生成工具（DES）** | 规模+数据：构造能生成接近真实企业业务环境与数据的系统（机构/业务流程/ERP/财务/多表），供演示与测试 | 🆕 Jack 新增 ⭐；**形态已定**（2026-08-21） |

> 议题 9 与议题 5（真实场景试用）互为表里：9 是"造一个接近真实的企业"当练兵场，5 是把练兵成果拿到真实场景验证。

## 2. 议题 8：本体论落地实施方法论（Jack 新增）

- 定位：OntoRun 研究面的"方法论"切面（方向宪法 §5 切面①），S1 已产出白皮书 §7（本体构建方法论），S2 需要把它从"项目内经验"提炼为"可复述、可迁移、经得起同行挑刺的方法论"。
- 待讨论：
  - 方法论的粒度与形态（步骤/决策点/反模式/验收判据？）；
  - 与白皮书、方向宪法"脊柱之问"（为 Agent 而建的建模范式）的关系；
  - 是否沉淀为可执行流程（工具/脚本）还是文档方法论（先文档后工具？）。

## 3. 议题 9：真实企业生成工具（Jack 新增，⭐）

**问题**：OntoRun 需要"接近真实企业的业务环境和数据"做演示与测试，而 S1 的 `data/seed_retail_source.py` 是**手工确定性脚本**（300 客户/60 商品/2200 订单 + 手工 corner case），不是"能生成一个企业"的工具。

**Jack 原意**：构造一个系统/工具，可以**把一个企业的真实业务流程、机构、ERP、财务系统、各种各样的表和数据真实地产生出来**，生成接近于真实企业的业务环境和数据信息，用于 OntoRun 的演示和测试。

**第一性原理拆解**（待 Jack 拍板的方向，非结论）：
- 核心矛盾：真实企业 = 业务（流程）+ 组织（机构）+ 系统（ERP/财务）+ 数据（多表、脏、有 corner case）。S1 只覆盖了"零售供应链交易链"一张网。
- 形态候选：
  - A. **企业生成器（Generator）**：配置化的企业"骨架"（行业/组织/流程/系统清单）→ 生成库表 + 带业务语义的数据（确定性+可复现），类似 S1 seed 脚本的规模化泛化；
  - B. **场景模拟器（Simulator）**：数据不是一次生成，而是按业务流程"运转"产生（时间推进、状态流转、跨系统写回），接近真实运行的 ERP；
  - C. **合成数据工厂**：偏数据侧，用生成模型/规则产出大规模、多脏度、多 corner case 的表数据。
- 与 OntoRun 的关系锚点（防目标漂移）：生成工具是"⑤ 数据/源系统"层的弹药——让本体运行时有更真实、更复杂的对象/链接/动作空间去治理；本身不是研究对象（不造数据治理轮子）。
- 与议题 5（真实场景试用）关系：生成企业当"练兵场"，真实场景当"验收场"。

## 3.5 议题 9 形态结论（2026-08-21，Jack 拍板）

1. **DES 定位** = OntoRun 模块 `des/`（非独立项目），先做 P0 最小集（YAML 配置 + 数据表生成 + 问题注入），3-4 周；
2. **本体映射做实**：维度 7（问题→本体概念）第一阶段必做——DES 产出的问题必须能对上 OntoRun 本体，防断链；
3. **技术降配**：SQLite + Python 标准库 + pydantic（复用现有栈）；砍 pandas/pyarrow/SimPy/NetworkX/Mimesis；
4. **SQLite 性能实测**（本机 100 万行+5 万客户，123MB）：插入 9.3s、等值 3ms、join 30ms、全表 SUM 67ms、TOP10 聚合 2.3s——覆盖 DES 目标量级（25 万行/季度）4 倍以上；
5. **量级真相**：真正瓶颈是无预聚合的即时大聚合（2.3s），反哺议题 1（ChatBI 需预聚合/物化视图）；SQLite 承载预聚合表没问题；
6. **规模策略**：1 企业 = 1 库（规避单库膨胀）；发布期 SQLite → PostgreSQL（schema 层预留）。

## 3.5 议题 1 形态结论（2026-08-21，Jack 拍板 v2）

1. **预聚合优先**：分析指标先物化（成熟数仓范式 + DuckDB/dbt 语义层），LLM/本体查的是**物化结果**，不是裸表现场算——继承 Jack 数仓"空间换时间"经验，Palantir 亦然（官方区分 pre-computed vs dynamically derived，<~1 万对象/查询才用动态派生）；
2. **本体读侧 = 物化指标的语义入口**：本体对象可"指向"预聚合指标（对象→指标定义→物化结果），Agent 在本体上选择，落到预聚合层执行；本体不取代数仓，是给数仓成果加语义外壳；
3. **不造轮子**：物化/维度建模用成熟范式（dbt 语义层/DuckDB），OntoRun 只做"本体↔物化指标"映射；
4. **Palantir 对照**：其查询机制 = Agent 在本体上"选 object set → 过滤/聚合/加载"，对象被索引进对象库（OSv2/Funnel）；预聚合走 pipeline，非查询期现场算；
5. **分析引擎选型**：DuckDB（成熟 OLAP 嵌入库，列存/向量化）作 ChatBI 分析层；SQLite 保留作源系统/写回层（S1 不变）；不用 SAP HANA 内存库（MVP 过度设计，DuckDB 即"穷人版分析内存库"）。

## 3.7 议题 2 形态结论（2026-08-21，Jack 拍板）

**问题本质**：S1 靠手写注册表（8 对象/8 链接/6 动作）；S2（DES 真实企业 30-50 表/几十对象动作）下手写必崩。核心难点 = **业务数据 → 本体映射**（Palantir FDE 最有价值部分），但中小企业项目养不起重人力 → 折中 = 工具自动化 + 轻人工审核。

**形态**：
1. **映射对象 = 两者都要（C）**：DES 生成数据（语义已知，半自动映射）+ 外部导入真实数据（语义未知，自动推断 + 审核）。演示用 DES，交付接真实数据——两者不内部绑定（未来落地数据都不是 DES 生成的）；
2. **映射自动化 = 分级置信度 + 轻人工审核**：builder 现有映射能力（FK 检测/命名对齐/LLM 提取）+ DES 自带语义 → 映射候选 + 置信度；**高置信度自动过，低置信度进审核队列**；人只审可疑项（~80% 自动 + ~20% 人审），人从不从零建模；
3. **映射审核工作台（Q2）**：本体驱动 UI 一页式 = 映射建议队列（源表/字段 → 候选对象/属性/链接 + 置信度）+ 批量接受/拒绝 + 冲突标记（同字段映射二义性高亮）+ 人工修正（改完即入注册表）+ 审核痕迹（对接议题 3 权限/审计）；不新造轮子，扩展现有 builder 页面（React+AntD）；
4. **动作规模化**：动作白名单保留 + **动作模板库**（create_order/cancel_order 家族做成可复用模板，新动作从模板派生）；
5. **校验规模化**：分两层 = 通用约束（自动套用：金额≥0/枚举合法）+ 业务规则（模板携带或人工），用"校验注册表"管理、可机验。

## 3.9 议题 3 形态结论（2026-08-21，Jack 拍板）

**问题本质**：S1 演示环境无鉴权；S2 要"可信"——谁能读/写/审，动作被审计。

**形态**：
1. **主体 = 双主体**（Agent 操作 + 人审核工作台/规则制定）；未来演进单主体理想态保留（系统价值 = 人定规则机器执行，非机器替代人）；
2. **客体 = 读/写/审三分治**：读（ChatBI 只读查询，不碰源库）、写（动作写回，S1 已有门控）、审（映射审核，人专属）；
3. **粒度 = 对象级 + 属性（列）级**，角色级作便捷分组（不够用）；**权限做在语义接口层**（本体对象/链接/动作挂权限策略），不依赖数据库层——因 SQLite 原生无行/列级权限、DuckDB 有 rls/table_guard 社区扩展但有限，语义层承载才匹配"本体即治理"（Palantir 同：对象类型级 + 属性级安全策略）；
4. **载体**：权限策略挂本体元数据（对象类型→谁能读/写），审核痕迹对接审计（议题 2 工作台天然接入）；
5. **不造轮子**：复用现有 FastAPI 鉴权 + 审计框架；字段级如需可引 DuckDB rls 扩展（成熟件）。

## 3.11 议题 4 形态结论（2026-08-21，Jack 拍板）

**现状**：前端 localStorage 已做 MVP（TD-15）；后端 session 内存态（src/app/session.py）。

**形态**：
1. **存什么**：会话消息 + 会话状态（选中对象/上下文）+ 审计关联（会话 id 挂动作审计，对接议题 3）；
2. **存哪**：**SQLite 单表**（sessions + messages，复用现有双库）；不引 Redis（发布期多实例才需要，S2 单机 SQLite 够，符合不造轮子+轻量）；
3. **TTL**：30 天清理；会话 id 串审计，可追溯"谁在什么会话做了什么"。

## 3.12 议题 4 状态

- 状态：**形态已定**
- 依赖：S1 session 模块改造 + 议题 3 审计关联

## 3.14 C 档形态结论（2026-08-21，Jack 拍板）

1. **#5 真实场景试用** = DES 先行、真实后迁：先让 DES 跑起来当"模拟真实场景"，再迁移到 Jack 真实数据（与议题 2 双通道一致）；Jack 提供真实候选场景待 DES 落地后衔接；
2. **#6 CI + UX** = GitHub Actions 权限确认后真跑（ci.yml 已 active）；UX 打磨范围待 Jack 试用反馈；
3. **#7 一键起** = scripts/start_dev.sh：读 ~/.dsh/.credentials.yaml 的 DEEPSEEK_API_KEY → 起后端(:8000)+前端(:5173)，直接做。

## 3.17 议题 8 形态结论（2026-08-21，Jack 基本同意，世界级难题不锁定）

**定位（Jack 原话要义）**：OntoRun 的立项目标之一 = 落地研究本体论落地方法论；Palantir 方法论"大家都在解读、赛道激烈"，但**如何落地、有实战经验的人全国极少**——补业界缺失内容 = 学位级作品。宏观宏大，需充分调研（尤其 Palantir 方式）+ 必要裁剪。

**形态（三层，Jack 认可）**：
1. **文档方法论**：白皮书 v2 骨架，基于浓缩版六阶段（阶段0-5：准备/业务语义建模/数据映射治理/Ontology构建/应用赋能/运营迭代）+ 十条铁律 + 反模式 Top15 + 十个开放张力点；加**中小企业裁剪**（Palantir 面向百亿级项目，OntoRun 面向几千万级）；
2. **可执行规范**：沉淀为 DES 配置规范/映射审核流程/权限模型/契约校验，随系统落地（落地先行，方法论从工具里长出来）；
3. **方法论交互 UI**（业界空白，差异化点）：每一步展示 输入/输出/为什么/在系统里怎么跑；**绑定真实运行系统**（点击看"这一步在系统里怎么跑"），非静态图文——防"数字盆景"（反模式 9 翻版）。

**核心主张一句话**："大模型要操作企业，必须先有现实世界的语义层当接口"——本体是运行期语义层，落地方法论 = 数据→本体→Agent 的映射与治理闭环 + 中小企业裁剪。

**对浓缩版三个关键张力点的立场**：
- T1（平台内置 vs 拼装）：轻量平台——本体运行时+语义接口自研（研究本身），数据管道/传输/前端成熟件拼装；方法论 UI = 第一个"平台化"对外的脸；
- T5（Action 写回）：S1 已验证，S2 明确搁置（Jack 拍板先 ChatBI）；方法论写"写回是闭环关键"但系统 S2 不实现——**冲突显式记录**，方法论=北极星，系统=当前能力；
- T10（种子用户兼作方法论验证）：**DES 生成企业 = 我们的"种子用户"**，跑通即方法论验证——DES→映射→ChatBI→方法论闭环 = 别人没有的实战证据。

**状态**：形态基本定（世界级难题，细节待全量复核 + Palantir 文档通读后精化）

（Jack 补充 2026-08-21）

**难点本质（Jack 原话要义）**：难的不是"模拟数据发生器"，是"模拟真实企业"本身：
1. **多系统模拟**：企业有 ERP/财务（如用友，仅举例）/CRM/WMS/订单系统等多个系统，需**按系统分别生成**，每个系统的表结构**符合行业最佳实践/规范**（行业是否有通用标准待调研）——足够真实是难点；
2. **业务流程驱动**：必须先模拟企业业务流程，才知道数据怎么生成（流程 → 数据逻辑）；
3. **问题注入** + **本体映射**：更难（问题要站得住、映射要经得起挑刺）。

**风险定级**：技术风险低（SQLite 实测覆盖），**质量/内容风险高**（真实感验收靠 Jack 业务经验 + Rose 调研兜底）。

**推进策略（已对齐）**：DES 不一步到位——先做最小垂直切片（1 张主数据表 + 1 个问题 + 1 条映射，跑通全链路），再横向铺开；A 档剩余为方法论（#8），DES 是最核心依赖，优先度最高。

（Jack 指出 2026-08-21）

- DES（议题 9）是 #5 前置（没数据一切白搭）、#1 弹药（ChatBI 消费 DES 数据）、#2 练兵场（映射工作台跑 DES 数据）；
- 标记为 **S2 最高风险项**：工作量最大（3-4 周 P0）+ 质量要求高（真实感/问题注入/本体映射）+ 是其他议题的公共依赖；
- 策略：DES 形态已定（SQLite + 标准库 + 问题注入 + 本体映射做实），**建议 DES 尽早启动（甚至先于 A 档剩余议题细化）**，避免成为关键路径瓶颈。

（2026-08-21）

- #2 规模化治理（形态已定）、#3 权限（形态已定）、#4 会话持久化（形态已定）
- A 档：#9 DES（形态已定）、#1 ChatBI（形态已定 v2）、#8 方法论（A 档最后一站，未定）
- 下一步：C 档（#5 真实场景 / #6 CI+UX / #7 一键起）→ 后回 A 档 #8 方法论



- 状态：**形态已定**
- 依赖：议题 1（读侧）、议题 2（审侧）、S1 审计框架



- 状态：**形态已定**
- 依赖：DES（议题 9）提供表源；builder（S1 P1-P3 映射能力）扩展；议题 3（权限：审核痕迹/动作权限）



- 状态：**形态已定**（预聚合优先 + 本体读侧物化指标入口 + DuckDB）
- 依赖：DES（议题 9）数据 → ChatBI 消费；与议题 3（权限：读侧分析治理）相关



- 状态：**形态已定**（细节、文件结构、交付顺序待编码阶段）
- 依赖：#1 ChatBI（DES 数据喂给 ChatBI 语义层）→ 顺序上 DES 先产出数据，ChatBI 后消费

## 3.18 第一轮全量复核（2026-08-21，4 模型对抗）

**方式**：workflow 派发 4 模型（deepseek-v4-pro/kimi-k2.7-code/doubao-seed-2.1/minimax-m3）独立对抗复核 9 议题形态 + Palantir 9 模块机制摘要；报告存档 research/s2-review-*.md（commit e308f2c）。

### 共识项（≥3 模型一致，已并入本清单）
- **C1 量级断层**：DES 25 万行 vs ChatBI PB 级 = S2 最大叙事裂缝 → **诚实降级 S2 量级目标为 100GB-1TB 级**（DuckDB 胜任），设量级 gate（100 万/1 亿行），DES 不宣称验证 PB；
- **C2 映射阈值**：80% 自动是拍脑袋数字 → S2 先做"建议+全人工审核"，跑 1-2 月数据后再定置信度阈值；必有映射变更影响分析；
- **C3 权限承载**：数据权限做在语义层与 Palantir 矛盾 → **数据权限下沉数据源**（PostgreSQL RLS/DuckDB 视图），S2 字段级降级为"对象级+属性级标注"，语义层只做操作权限+策略分发；
- **C4 双引擎一致性**：SQLite+DuckDB 同步机制未定义 → 定义数据流转契约 + reconcile 测试；显式声明 S2 单用户前提；
- **C5 审计独立**：审计与会话耦合 + TTL 30 天不合规 → 审计独立模块（WORM 存储+动作级+字段级镜像），TTL 按数据分级；审计独立于会话存储；
- **C6 方法论分期**：反模式 Top15 先写=违反落地先行 → 方法论分两阶段：S2 只做白皮书 v2 骨架 + 方法论 UI（绑定真实运行），反模式从落地后提取。

### 分歧项（Rose 已给证据推荐，Jack 确认/否决）
- **D1 DES 技术栈**：推荐 = 核心数据生成用标准库（SQLite+pydantic）；"业务流程驱动数据生成"推迟 S3；S3 需要时用成熟状态机库（transitions），不引 SimPy/NetworkX；
- **D2 方法论 UI**：推荐 = S2 做最小"指针版"（每步链到真实运行输出/git commit），完整交互 UI 推迟 S3；
- **D3 ChatBI 查询形态**：推荐 = 采纳契约 v0.1（DES 切片 §8）+ 垂直切片内跑 head-to-head 实验（30 问题：NL2SQL+守卫 vs 受限结构化查询）再定终版；默认受限结构化查询，20% 冷问题可 LLM 生成经安全校验 SQL 兜底；
- 完整证据见 research/s2-review-decision-pack.md（commit a3599e1）。

### P0 风险清单（任一模型标 P0）
1. 量级断层（C1）；2. 映射 80% 拍脑袋（C2）；3. 权限承载（C3）；4. 结构化查询 schema 未定义（pro F4）；5. DuckDB 单机 vs PB（C1 变体）；6. 审计独立+合规（C5）；7. 真实场景 #5 单点失败（"待 Jack 提供"= 风险）。

### 本清单未决变更（待 Jack 拍板后正式落定）
- 上述 D1-D3 三项分歧，以及 C1 的"降级 100GB-1TB"是否采纳（这是对 S2 定位的重要修正，需 Jack 确认）。

## 3.19 前置决策拍板（2026-08-21，Jack 逐条确认）

| 决策 | 拍板 | 关键修正 |
|---|---|---|
| C1 量级 | ✅ 采纳 | **修正为 100 万行级**（够演示/本机可跑/GitHub 下载也能跑/利于传播；砍掉 1 亿行档；"100 万甚至有点多"）——本机测试环境规模决定，不搞 PB/1TB/1 亿 |
| D1 技术栈 | ✅ 采纳 | 标准库 + 流程驱动推 S3 |
| D2 方法论 UI | ✅ 采纳 | S2 指针版 |
| D3 查询形态 | ✅ 采纳 | 契约 v0.1 + head-to-head |
| #5 真实场景 | 🔄 兜底 | 这次按计划兜底（DES 当模拟真实场景）；单位场景留以后（不暴露单位名）；Rose 可上网搜候选 |

**量级定论**：S2 目标 = **DES 生成 100 万行级数据**（本机可演示、可传播、GitHub 下载可跑）；不设 1 亿行档；量级 gate 简化为单档（100 万行级达标）。

## 4. 待 Jack 拍板

1. 议题顺序：1 → 2 → 3/4 → 8/9 并行？还是先 8（方法论）定调再往下走？
2. 议题 9 形态：A（生成器）/ B（模拟器）/ C（合成数据）/ 组合？规模到哪一档（企业数/表数/数据量）？
3. 议题 5 候选真实场景：有无具体企业/行业/场景可给？
4. 议题 8 的产出形态：文档方法论 vs 可执行流程工具？

## 5. 关联文件

- 方向宪法：`方向与战略_v0.2.md`（§3 脊柱之问、§5 三切面）
- S1 白皮书：`docs/白皮书_v0.1.md`（§7 本体构建方法论）
- ChatBI 方向：`docs/ChatBI-智能分析-方向_v0.1.md`（⏳ 待系统规划）
- 现有数据生成器：`data/seed_retail_source.py`（议题 9 的起点）
- 技术债：`docs/tech-debt.md`（TD-14/15 开放）
- 版本基线：S1 = tag `v0.1.0`（本地 + GitHub 均已打，远端 main=ddf0e17e）