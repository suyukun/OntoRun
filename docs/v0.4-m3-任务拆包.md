# S4 M3 任务拆包书（内部派活文件）

> M2（规则注册）落地后立即派发，两任务并行（≤2 并发纪律）。验收基准：口径包 v0.3 §七七幕剧本端到端可跑 + §九 终极验收条款。

## M3a · Agent/后端（依赖 M2 产物）

1. **对话唯一入口**：Agent 对话成为一级 API 入口；查询答案统一附**证据链载荷**（表名/规则名/分母/明细行引用——真实数据联动，禁 mock，红队玩具分水岭条款）。
2. **剧本七幕 API 支撑**：
   - 第 0 幕现状对照：静态叙事素材端点（或前端常量）
   - 第 1 幕揭示：联合授信台账归集查询（银行 8.0%/证券 5.5%/资管 8.2% 逐家→合计 10.8% 橙）
   - 第 2 幕升级识别：恒昌贸易三线索查询（股权代持/交叉担保/资金往来）+ 纳入重算 12.8% 红（消费 M2 引擎）
   - 第 4 幕双签驳回：AI 提议「拆分授信至非关联通道主体」（agent prompt 注入剧本设定）→ approve_disposal 驳回路径（2023 办法第二十三条依据文案）→ 退回重新起草
   - 第 5 幕监管动作：红色预警触发**报送初稿生成**（大额风险暴露口径，2018 办法 37 条）
   - 第 6 幕闭环+看板：瑞华黄档完整解除链 + 全局督办看板聚合端点（前十大集团集中度、待确认/超期督办数、附属机构响应时效）
3. **约束**：不动 33 对象 schema；增量测试（禁全量 pytest）；bash 显式 timeoutMs；口径断言回链口径包。
4. **必读**：口径包 v0.3、方向重锚、M2 交付报告与引擎路径核对输出、scripts/verify_demo_numbers.py。

## M3b · 前端（可与 M3a 并行，按上述 API 契约开发，尾段联调）

1. **信息架构收敛**：对话 = 唯一一级入口（`/` 直达）；数据浏览/图谱/DES 撤出门面（图谱等收进证据链展开态）。
2. **剧本交互帧**：第 0 幕对照帧、第 1 幕揭示（逐家→合计动效）、第 2 幕三线索展开、第 4 幕驳回 UI（驳回理由展示「隐匿关联拆分交易」条款）、第 5 幕报送初稿+监管动作面板、第 6 幕全局督办看板。
3. **中文枚举渲染**：黄/橙/红三色徽标（浅色金融风沿用 riskTheme）。
4. **约束**：vitest 增量 + tsc；异步快照类用例 vi.setConfig testTimeout 20s（既有范式）；不改后端。

## 联调与收尾（M3 尾，主会话执行）

- 重启后端（数据再生后 uvicorn 引擎需重建）；七幕剧本逐幕端到端手测；核对脚本终跑。
- 已知环境：项目 venv = /opt/anaconda3/bin/python3（.venv 无依赖）；长驻服务 nohup+disown。
