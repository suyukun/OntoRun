# S2 P1.5 治理骨架完成记录（2026-08-21）

> 编制：Rose ｜ 状态：P1.5 三骨架实现 + 门禁全过，阶段末全量验证中
> 依据：S2 开发计划 v0.3（P1.5 门禁）+ P1.5 设计 v0.1（commit f4dbb0f）

## 一、P1.5 目标与门禁
P1.5 = 权限元数据 schema + 审计最小骨架（append-only）+ 映射置信度打标骨架；P2/P3/P4 的公共地基。
门禁：权限元数据可写 · 审计骨架可查 · 映射打标骨架可跑。

## 二、交付物（commit 9931bb8）
| 骨架 | 实现 | 门禁验证 |
|---|---|---|
| ① 权限元数据 | src/runtime/permissions.py：PermissionService CRUD + PermissionRegistry + decide() 纯函数 + V1-V9 写时机验 + permission_roles/policies 表 | **可写**：CRUD + V1-V9 拒绝（未知对象/属性/非法操作/approve-给-agent/属性级带-write/未知 role/重复 id）+ decide fail-closed/deny-wins/属性级剔除 ✅ |
| ② 审计最小骨架 | src/runtime/audit.py 扩展：SHA-256 哈希链（prev_hash/record_hash + verify_integrity）+ audit_field_mirror 字段镜像 + WORM 触发器（BEFORE UPDATE/DELETE → RAISE）+ retention_class/source 分级 + 独立于会话 | **可查**：哈希链正确性 + 旁路篡改（删触发器改数据）检出 broken + WORM 拒绝改删 + 镜像与 effects 一一对应 ✅ |
| ③ 映射打标骨架 | src/builder/mapping/annotate.py：MappingCandidate + classify()（0.9/0.6 阈值可配）+ routing()（高自动过/低进队列）+ 审核状态机（approved 终态）+ S1 builder 适配器（fk_detection/alias_matcher/naming/DES score 公式）+ history append-only | **可跑**：档位自洽 + 高置信度 auto_approved + 中/低 draft + 非法流转拦截 + target 未注册拒绝 ✅ |
| ④ 自检 | src/runtime/governance_checks.py：GovernanceValidator（权限/映射引用一致性）经 Registry.add_self_check 挂载 | 未知 target 策略/候选 → error 级检出 ✅ |

store 层：GOVERNANCE_SCHEMA（5 表 + WORM 触发器 + 索引 + 枚举单一来源派生 CHECK + audit_log 幂等加列 4 项，不 bump schema_version，沿用 v2/v3/v4 先例）。

## 三、门禁测试
tests/test_p15_governance.py：**49 passed**（权限可写 / 审计可查 / 映射可跑 / GovernanceValidator）。

## 四、验证
- 冒烟（我跑）：P2 decide 正确（allow/fail-closed）、P3 哈希链 verify ok + 旁路篡改检出、P4 打标 auto/队列/非法流转拦截 全过
- 既有回归（审计/注册表/映射相关）：133 passed 零回归
- 阶段末全量 pytest：验证中
- ruff 全绿

## 五、接线预留（P2/P3/P4）
- P4 动作执行：ActionEngine.execute() ①与④之间插权限门位（decide write/approve），PERMISSION_DENIED 错误码 P4 开工前确认
- P2 契约执行：读侧 decide(read) + visible_attributes 过滤返回列
- P3 映射审核：工作台批准 = decide(approve, human) + review_history/audit
- 审计修正策略（设计 R3）：只追加修正记录不 UPDATE 原记录——**待 Jack 确认**

## 六、待确认 / 后续
- R3 审计修正策略（append correction 而非 UPDATE）待 Jack 拍板
- PERMISSION_DENIED 错误码（改本体 schema 边界）P4 开工前与架构确认
- 下一步 P2 ChatBI 闭环（本体读侧 + DuckDB 物化 + head-to-head 30 问）
