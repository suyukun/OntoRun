# 财富广场·数仓输入准备清单 v0.2（注册场景，一页）

> 拿这份跟负责注册的同学过一遍，过完直接派活。**四样全是"拷现成的东西"，不写文档、不答题、不整理。**
> 配套方法论：`docs/财富广场-注册场景middle-out落地方案_v0.2.md`（不用读）。

## 要的四样东西

| # | 拷什么 | 从哪拷 | 注意 |
|---|---|---|---|
| A | 建表语句/字段清单：`dwd_tr_rgst_df`、`dim_cu_usr_info_df`、`dim_ch_chl_df` + 注册相关的 DWS 表（`dws_reg_*` 有几张带几张） | DWS 建模工具 / DDL 导出 | 原样导出即可 |
| B | 上述表的上下游**正式调度 SQL**（`INSERT INTO ... SELECT ...`） | 调度平台脚本 | 在跑的正式任务；临时补数/修复脚本不要 |
| C | 核心三张表**数据导出 CSV**，时间窗 2026-07-01 ~ 2026-08-31 | 查询导出 | **手机号/证件号打码**，其余字段原样不动 |
| D | 注册相关的**报表 SQL**（ADS 层，一张报表一条） | 报表平台 | 有几张给几张，散的更好——每张都是口径证据 |

## 忙不过来的兜底

先给 **A + C**（够建模+对账开工），B、D 随后补，不卡进度。

## 交付方式

拷给 Jack 即可（文件夹/压缩包/微信传文件都行），**不用整理、不用命名规范**。落点：`materials/fortune-warehouse-input/`。

## 现在不用做的（明确排除）

- **不用回答任何字段/口径问题**——Rose 消化完上面四样后，会出一份**是非题/选择题清单**（例："实名完成 = real_name_status=2 且 real_name_time 非空，对吗？"），到时勾对错就行；
- 不用写说明文档、不用解释表关系（血缘 Rose 从 B 的 SQL 里解析，对不上的用 C 的数字反推）。

---
v0.2 2026-09-09。取代 v0.1（commit 4202137）。修正记录：v0.1 误设 Jack 为"转发消息的传声筒"、消息含五问调研式采集；实际 Jack 即数仓负责人，v0.2 改为直接派发的四样"拷现成"清单 + 后续是非题问答，采集成本压到最低。
