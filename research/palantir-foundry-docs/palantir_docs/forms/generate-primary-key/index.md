来源: https://palantir.com/docs/zh/foundry/forms/generate-primary-key/

# 生成主键

注意：以下翻译的准确性尚未经过验证。这是使用AIP ↗从原始英文文本进行的机器翻译。

# 生成主键

Foundry Forms 不再是 Foundry 上数据输入或数据输出工作流的推荐方法。相反，使用 Foundry Ontology 搭建用户输入工作流，将相关数据结构表示为 object 类型，并通过操作配置数据输出交互。在Forms 概述文档中了解更多信息。

主键是唯一标识数据集中每一行的值。通过 Fusion 支持的表单，用户可以定义或自动生成这样的值。

理想情况下，写入主键列的字段不应是可编辑的。相反，该字段应自动生成一个唯一值（隐藏或其他形式），例如时间戳或其他字段的连接。

设置主键的步骤：

- 在表单配置至少一个字段后，选择响应数据选项卡。
- 在面板底部找到表格列部分。
- 选择目标字段右侧的钥匙图标。
如果主键字段没有值，将自动生成一个唯一的值。

如果主键字段的值不唯一，仍会创建新行，但列将不再具有唯一值。
