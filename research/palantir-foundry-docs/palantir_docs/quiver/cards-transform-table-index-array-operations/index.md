来源: https://palantir.com/docs/zh/foundry/quiver/cards-transform-table-index-array-operations/

# 数组操作

注意：以下翻译的准确性尚未经过验证。这是使用AIP ↗从原始英文文本进行的机器翻译。

# 数组操作

返回到:变换索引

本节中的变换操作于数组数据类型。
了解更多关于如何搜索并添加变换。

- 创建数组
- 拆分
- 唯一值
- 索引处的值
- 数组合并
- 数组排序
- 数组包含
- 步进数组
- 数组起始
- 数组结束
- 数组长度
- 数字数组聚合
- 字符串转换为数字
- 字符串转换为布尔值
- 字符串转换为日期
### 创建数组

通过手动添加值或引用现有数组来定义一个数组。

### 拆分

从字符串输入返回一个以分隔符字符串拆分的字符串数组。

### 唯一值

返回一个包含输入数组中唯一值的数组。

### 索引处的值

返回输入索引处的值。

### 数组合并

通过组合输入字符串数组值返回一个字符串（字符串分隔符非必填）。

### 数组排序

对数组进行排序。

### 数组包含

如果输入数组包含指定值则返回true，否则返回false。

### 步进数组

从起始值、结束值和步长返回一个数字数组。

### 数组起始

返回数组中的第一个值。

### 数组结束

返回数组中的最后一个值。

### 数组长度

返回数组的长度。

### 数字数组聚合

对数字数组执行各种单一数字聚合。

### 字符串转换为数字

将一个字符串数组转换为一个数字数组。

### 字符串转换为布尔值

将一个字符串数组转换为一个布尔值数组。

### 字符串转换为日期

将一个字符串数组转换为一个日期数组。
