来源: https://palantir.com/docs/zh/foundry/quiver/core-concepts/

# 核心概念

注意：以下翻译的准确性尚未经过验证。这是使用AIP ↗从原始英文文本进行的机器翻译。

# 核心概念

Quiver 基于以下核心概念构建：

- 分析Quiver 具有两种主要模式进行分析：画布和图形。了解有关 Quiver 中分析的更多信息。
- Quiver 具有两种主要模式进行分析：画布和图形。
- 了解有关 Quiver 中分析的更多信息。
- 卡片在 Quiver 中，卡片是用于指代分析中任何内容（例如可视化）的通用术语。了解有关 Quiver 中卡片的更多信息。
- 在 Quiver 中，卡片是用于指代分析中任何内容（例如可视化）的通用术语。
- 了解有关 Quiver 中卡片的更多信息。
- 数据模型Quiver 中的每个卡片或变换都需要一个或多个必需的输入数据类型，并生成具有确定数据类型的输出。Quiver 的数据模型定义了这些数据类型。了解有关 Quiver 数据模型的更多信息。
- Quiver 中的每个卡片或变换都需要一个或多个必需的输入数据类型，并生成具有确定数据类型的输出。Quiver 的数据模型定义了这些数据类型。
- 了解有关 Quiver 数据模型的更多信息。
- 仪表盘Quiver 仪表盘是只读的、交互式的视图，包含分析中的图表和数据。Quiver 仪表盘可以作为独立仪表盘使用或嵌入其他 Foundry 应用中。了解有关仪表盘的更多信息。
- Quiver 仪表盘是只读的、交互式的视图，包含分析中的图表和数据。Quiver 仪表盘可以作为独立仪表盘使用或嵌入其他 Foundry 应用中。
- 了解有关仪表盘的更多信息。
- 可视化函数可视化函数是使用 Quiver 的点击界面构建的可重用逻辑块。这些函数可以在同一个或其他 Quiver 分析中多次使用，并与其他用户共享以用于他们自己的 Quiver 分析。了解有关可视化函数的更多信息。
- 可视化函数是使用 Quiver 的点击界面构建的可重用逻辑块。这些函数可以在同一个或其他 Quiver 分析中多次使用，并与其他用户共享以用于他们自己的 Quiver 分析。
- 了解有关可视化函数的更多信息。