来源: https://palantir.com/docs/zh/foundry/quiver/cards-transform-table-index-string-operations/

# 字符串操作

注意：以下翻译的准确性尚未经过验证。这是使用AIP ↗从原始英文文本进行的机器翻译。

# 字符串操作

返回到：变换索引

此部分中的变换作用于字符串数据类型。了解更多关于如何搜索并添加变换。

- 子字符串
- 小写
- 大写
- 连接
- 查找和替换
### 子字符串

从给定的起始索引开始，并具有给定长度的字符串的子字符串。

### 小写

返回转换为全小写的输入字符串值。

### 大写

返回转换为全大写的输入字符串值。

### 连接

返回一个连接两个输入字符串的字符串（字符串分隔符非必填）。

### 查找和替换

将匹配的字符串值替换为不同的字符串。
