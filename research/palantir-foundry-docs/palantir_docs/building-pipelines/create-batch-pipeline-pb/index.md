来源: https://palantir.com/docs/zh/foundry/building-pipelines/create-batch-pipeline-pb/

# 使用 Pipeline Builder 创建数据集批处理管道

注意：以下翻译的准确性尚未经过验证。这是使用AIP ↗从原始英文文本进行的机器翻译。

# 使用 Pipeline Builder 创建数据集批处理管道

在本教程中，我们将使用 Pipeline Builder 创建一个简单的管道，输出包含航班警报信息的单个数据集。然后，我们可以使用 Contour 或 Code Workbook 等工具分析此输出数据集，以回答哪些航班路径存在最大中断风险等问题。

以下使用的数据集可以在数据集导入步骤中按名称搜索，并可以在您的 Foundry 文件系统中的 Foundry 参考项目中找到：Foundry Training and Resources/Foundry Reference Project/Tutorial Reference Examples/Track: Data Engineering/Datasource Project: Flight Alerts/datasets。

在本教程结束时，您将拥有一个如下所示的管道：

该管道将生成一个新的数据集输出Flight Alerts Data，可以用于进一步探索。

## 第1部分：初始设置

首先，我们需要创建一个新的管道。

- 登录 Foundry 后，从左侧导航栏下的应用程序中访问Pipeline Builder。如果没有显示，请点击查看全部并在搭建与监控管道部分中找到Pipeline Builder。
登录 Foundry 后，从左侧导航栏下的应用程序中访问Pipeline Builder。如果没有显示，请点击查看全部并在搭建与监控管道部分中找到Pipeline Builder。

- 接下来，在 Pipeline Builder 登陆页面的右上角，点击新建管道来创建一个新管道。选择批处理管道。
接下来，在 Pipeline Builder 登陆页面的右上角，点击新建管道来创建一个新管道。选择批处理管道。

并非所有 Foundry 环境都支持创建流式管道。如果您的应用案例需要，请联系您的 Palantir 代表以获取更多信息。

- 选择一个位置来保存您的管道。请注意，管道不能保存在个人文件夹中。
- 点击创建管道。
## 第2部分：添加数据集

现在我们可以将数据集添加到我们的管道工作流中。对于本教程，我们将使用概念性或开源数据的示例数据集，所有数据集都应该作为 Foundry 参考项目的一部分在您的 Foundry 文件系统中可用。

在 Pipeline Builder 页面上，从 Foundry 点击添加数据集。

或者，您可以从计算机拖放一个文件以用作您的数据集。

在我们的演练示例中，我们将添加passengers_preprocessed、flight_alerts_raw和status_mapping_raw数据集。要添加数据集的选择，请选择数据集并点击行内的+图标，或点击添加到选择。

选定所需的所有数据集后，点击添加数据集。

## 第3部分：清理数据

添加原始数据集后，我们可以执行一些基本的清理变换以继续定义我们的管道。我们将对三个原始数据集进行变换。

### 数据集1

首先，让我们清理passengers_preprocessed数据集。我们将从设置一个转换变换开始，将dob列名更改为dob_date，同时将值转换为 MM/dd/yy 格式。

#### 转换变换

- 点击图中的passengers_preprocessed节点。
点击图中的passengers_preprocessed节点。

- 点击变换。
点击变换。

- 从下拉菜单中搜索并选择转换变换以打开转换配置面板。
从下拉菜单中搜索并选择转换变换以打开转换配置面板。

- 从表达式字段中选择dob，在类型中选择Date。
从表达式字段中选择dob，在类型中选择Date。

- 输入MM/dd/yy作为格式类型。请确保使用大写MM以确保转换变换成功。将输出列名更改为dob_date。您的转换面板应如下所示：
输入MM/dd/yy作为格式类型。请确保使用大写MM以确保转换变换成功。将输出列名更改为dob_date。

您的转换面板应如下所示：

- 点击应用将变换添加到您的管道中。
点击应用将变换添加到您的管道中。

#### 标题大小写变换

现在我们将格式化flyer_status列的值，使其以大写字母开头。

- 在变换搜索字段中搜索并选择标题大小写变换以打开标题大小写配置面板。
在变换搜索字段中搜索并选择标题大小写变换以打开标题大小写配置面板。

- 在表达式字段中，从下拉菜单中选择flyer_status列。您的标题大小写面板应如下所示：
在表达式字段中，从下拉菜单中选择flyer_status列。

您的标题大小写面板应如下所示：

- 点击应用将变换添加到您的管道中。
点击应用将变换添加到您的管道中。

- 在变换配置窗口的左上角，将变换重命名为Passengers_Clean。
在变换配置窗口的左上角，将变换重命名为Passengers_Clean。

- 点击右上角的返回到图表以返回到您的管道图表。
点击右上角的返回到图表以返回到您的管道图表。

### 数据集2

现在，让我们清理flight_alerts_raw数据集。首先，我们将设置另一个转换变换以将flight-date列的值转换为MM/dd/yy格式。

#### 转换变换

- 点击图中的flight_alerts_raw数据集节点。
点击图中的flight_alerts_raw数据集节点。

- 点击变换。
点击变换。

- 从下拉菜单中搜索并选择转换变换以打开转换配置面板。您可以阅读选择框右侧列出的函数定义以了解更多关于函数的信息。
从下拉菜单中搜索并选择转换变换以打开转换配置面板。您可以阅读选择框右侧列出的函数定义以了解更多关于函数的信息。

- 在表达式字段中，从下拉菜单中选择flight_date列。
在表达式字段中，从下拉菜单中选择flight_date列。

- 从类型字段下拉菜单中选择Date。
从类型字段下拉菜单中选择Date。

- 输入MM/dd/yy作为格式类型。请确保使用大写MM以确保转换变换成功。您的转换面板应如下所示：
输入MM/dd/yy作为格式类型。请确保使用大写MM以确保转换变换成功。

您的转换面板应如下所示：

- 点击应用将变换添加到您的管道中。
点击应用将变换添加到您的管道中。

#### 清理字符串变换

现在，我们将添加一个清理字符串变换，该变换将删除category列值中的空格。例如，该变换将把delay···字符串值转换为delay。

- 从下拉菜单中搜索并选择清理字符串变换以打开清理字符串配置面板。
从下拉菜单中搜索并选择清理字符串变换以打开清理字符串配置面板。

- 在表达式字段中，从下拉菜单中选择category列。
在表达式字段中，从下拉菜单中选择category列。

- 勾选所有三个清理操作选项：将空字符串转换为 null将多个空白字符的序列减少为一个空白字符修剪字符串开头和结尾的空白您的清理字符串面板应如下所示：
勾选所有三个清理操作选项：

- 将空字符串转换为 null
- 将多个空白字符的序列减少为一个空白字符
- 修剪字符串开头和结尾的空白
您的清理字符串面板应如下所示：

- 点击应用将变换添加到您的管道中。
点击应用将变换添加到您的管道中。

- 在变换配置窗口的左上角，将变换重命名为Flight Alerts - Clean。
在变换配置窗口的左上角，将变换重命名为Flight Alerts - Clean。

- 点击右上角的返回到图表以返回到您的管道图表。
点击右上角的返回到图表以返回到您的管道图表。

### 数据集3

最后，让我们清理status_mapping_raw数据集。

#### 清理字符串变换

我们将仅对该数据集应用一个清理字符串变换。

- 点击图中的status_mapping_raw数据集节点。
点击图中的status_mapping_raw数据集节点。

- 点击变换。
点击变换。

- 在搜索变换和列...字段中，从下拉菜单中选择mapped_value列。
在搜索变换和列...字段中，从下拉菜单中选择mapped_value列。

- 在同一字段中搜索并选择清理字符串变换。
在同一字段中搜索并选择清理字符串变换。

- 勾选所有三个清理操作选项：将空字符串转换为 null将多个空白字符的序列减少为一个空白字符修剪字符串开头和结尾的空白您的清理字符串面板应如下所示：
勾选所有三个清理操作选项：

- 将空字符串转换为 null
- 将多个空白字符的序列减少为一个空白字符
- 修剪字符串开头和结尾的空白
您的清理字符串面板应如下所示：

- 点击应用将变换添加到您的管道中。
点击应用将变换添加到您的管道中。

- 在变换配置窗口的左上角，将变换重命名为Status Mapping - Clean。
在变换配置窗口的左上角，将变换重命名为Status Mapping - Clean。

- 点击右上角的返回到图表以返回到您的管道图表。您可以看到刚刚添加的变换与应用它们的数据集之间的连接。
点击右上角的返回到图表以返回到您的管道图表。

您可以看到刚刚添加的变换与应用它们的数据集之间的连接。

## 第4部分：合并数据集

现在，让我们使用合并来合并一些已清理的数据集。合并允许您合并至少有一个匹配列的数据集。我们将在管道工作流中添加两个合并。

### 合并1

我们的第一个合并将合并两个已清理的数据集。

- 点击Flight Alerts - Clean变换节点。这将是合并的左侧。
点击Flight Alerts - Clean变换节点。这将是合并的左侧。

- 选择合并。
选择合并。

- 点击Status Mapping - Clean节点将其添加为合并的右侧。
点击Status Mapping - Clean节点将其添加为合并的右侧。

- 点击开始以打开合并配置面板。
点击开始以打开合并配置面板。

- 验证合并类型设置为左合并。
验证合并类型设置为左合并。

- 设置匹配条件列为status等于value。
设置匹配条件列为status等于value。

- 点击显示高级查看其他配置选项。
点击显示高级查看其他配置选项。

- 设置右侧Status Mapping - Clean数据集的前缀为status。您的合并配置面板应如下所示：
设置右侧Status Mapping - Clean数据集的前缀为status。

您的合并配置面板应如下所示：

- 点击应用将合并添加到您的管道中。
点击应用将合并添加到您的管道中。

- 在配置窗口底部的预览窗格中查看合并输出表的预览。
在配置窗口底部的预览窗格中查看合并输出表的预览。

- 在合并配置窗口的左上角，将合并重命名为Join Status。
在合并配置窗口的左上角，将合并重命名为Join Status。

- 点击右上角的返回到图表以返回到您的管道图表。
点击右上角的返回到图表以返回到您的管道图表。

- 为了使图表更易读，点击设计图标自动排列数据集或手动拖动两个连接的数据集到彼此旁边。
为了使图表更易读，点击设计图标自动排列数据集或手动拖动两个连接的数据集到彼此旁边。

### 合并2

对于我们的第二个合并，我们将合并第一个合并输出表与另一个原始数据集。

- 通过点击添加数据集将priority_mapping_raw数据集添加到图表中。
通过点击添加数据集将priority_mapping_raw数据集添加到图表中。

- 点击我们刚刚添加到图表中的Join Status节点。这将是合并的左侧。
点击我们刚刚添加到图表中的Join Status节点。这将是合并的左侧。

- 选择合并。
选择合并。

- 点击priority_mapping_raw数据集节点将其添加为合并的右侧。
点击priority_mapping_raw数据集节点将其添加为合并的右侧。

- 点击开始打开配置面板。
点击开始打开配置面板。

- 验证合并类型设置为左合并。
验证合并类型设置为左合并。

- 设置匹配条件列为priority等于value。
设置匹配条件列为priority等于value。

- 点击显示高级查看其他配置选项。
点击显示高级查看其他配置选项。

- 设置右侧priority_mapping_raw数据集的前缀为priority。您的合并配置面板应如下所示：
设置右侧priority_mapping_raw数据集的前缀为priority。

您的合并配置面板应如下所示：

- 点击应用将合并添加到您的管道中。
点击应用将合并添加到您的管道中。

- 在配置窗口底部的预览窗格中查看合并输出表的预览。
在配置窗口底部的预览窗格中查看合并输出表的预览。

- 在合并配置窗口的左上角，将合并重命名为Join (2)。
在合并配置窗口的左上角，将合并重命名为Join (2)。

- 点击右上角的返回到图表以返回到您的管道图表。
点击右上角的返回到图表以返回到您的管道图表。

您现在可以看到刚刚添加的合并与应用它们的数据集之间的连接。

## 第5部分：添加输出

现在我们已经完成了数据的变换和结构化，让我们添加一个输出。对于本教程，我们将添加一个数据集输出。

- 在 Pipeline Builder 图表右侧的管道输出侧栏中，将输出命名为Flight Alerts data。然后点击添加数据集输出。
在 Pipeline Builder 图表右侧的管道输出侧栏中，将输出命名为Flight Alerts data。然后点击添加数据集输出。

- 点击合并节点右侧的白色圆圈，将Join (2)连接到Flight Alerts data数据集。
点击合并节点右侧的白色圆圈，将Join (2)连接到Flight Alerts data数据集。

- 点击使用输入模式以使用现有模式。
点击使用输入模式以使用现有模式。

- 从这里，选择要保留的数据列。在我们的例子中，我们将保留所有数据。
从这里，选择要保留的数据列。在我们的例子中，我们将保留所有数据。

## 第6部分：搭建管道

要搭建您的管道，请确保点击保存，然后点击部署 > 部署管道。

您应该会看到一个小提示，指示部署成功。在提示框中点击查看以打开搭建进度页面。

从此页面，您可以监控搭建的进度，直到数据集输出准备就绪。

您现在可以通过点击操作 > 打开访问您的数据集。

通过最后一步，我们已经生成了我们的管道输出。此输出是一个数据集，可以在 Foundry 的其他应用程序中进一步探索，例如Contour或Code Workbook。
