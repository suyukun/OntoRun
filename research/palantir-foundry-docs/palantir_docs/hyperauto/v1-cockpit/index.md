来源: https://palantir.com/docs/zh/foundry/hyperauto/v1-cockpit/

# SDDI 控制台

注意：以下翻译的准确性尚未经过验证。这是使用AIP ↗从原始英文文本进行的机器翻译。

# SDDI 控制台

SDDI 控制台是配置和执行 SDDI 流程的中心位置，指导您从同步创建和配置到数据管道生成和 Ontology 创建。

控制台的左侧面板列出了各个 SDDI 流程步骤以及这些步骤的状态。点击每个步骤时，右侧主区域会显示您可以对该特定 SDDI 流程步骤执行的不同操作。

## 初始状态

在开始之前，需要将控制台与一个功能齐全的 SDDI 实例连接。
要配置您的第一个 SDDI 实例，请参阅入门。

## 访问现有控制台

要查看已配置的 SDDI 实例，请导航到数据连接中附加源的源概览页面，并选择编辑管道生成器设置。

## 控制台控件

控制台的左侧面板列出了三个阶段：

- 同步数据
- 管道生成
- Ontology 更改
### 同步数据

在同步数据阶段，您可以探索来自源的数据并创建新的同步到 Foundry。它还会自动配置 Foundry 中所需的元数据表，以确保 SDDI 正确运行。最后，它允许您触发将这些数据摄入到 Foundry。

### 管道生成

管道生成阶段允许您通过点选与 SDDI 自动管道生成器库进行交互，以生成和搭建数据管道，并导航到 SDDI 数据沿袭。

管道配置仅适用于非 SAP 源。对于 SAP 源，管道配置是在 SDDI SAP 源浏览器中设置的。

### Ontology 更改

在 Ontology 更改步骤中，您可以预览可以基于正在指定和处理的数据集批量进行的 Ontology 更改。

## 访问故障排除

从工作区导航栏下的软件定义数据集成中访问控制台。

权限可以通过控制面板的应用程序访问页面进行配置。如果您没有访问 SDDI 控制台的权限，请联系您的 Foundry 平台 IT 管理员或 Palantir 支持。
