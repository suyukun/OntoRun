来源: https://palantir.com/docs/zh/foundry/workshop/widgets-date-time-picker/

# 日期和时间选择器

注意：以下翻译的准确性尚未经过验证。这是使用AIP ↗从原始英文文本进行的机器翻译。

# 日期和时间选择器

日期和时间选择器微件被用于在允许用户输入单个日期和时间值。

## 配置选项

- 标签为微件设置一个非必填标签。此文本显示在微件的顶部。
- 为微件设置一个非必填标签。此文本显示在微件的顶部。
- 选定的时间戳微件的输出变量，存储用户选择的日期和时间值。
- 微件的输出变量，存储用户选择的日期和时间值。
- 日期格式设置微件显示的日期格式。
- 设置微件显示的日期格式。
- 时间格式设置微件显示的时间格式，可以使用12小时制或24小时制。
- 设置微件显示的时间格式，可以使用12小时制或24小时制。
- 时间精度设置微件使用的时间精度，可以精确到毫秒、秒或分钟。
- 设置微件使用的时间精度，可以精确到毫秒、秒或分钟。
- 时区用户可编辑控制微件的时区在查看模式下是否可由用户调整的切换。
- 控制微件的时区在查看模式下是否可由用户调整的切换。
- 默认时区设置微件使用的默认时区。可以通过手动选择时区静态设置，动态使用变量，或设为本地，使用只读者的本地时区。
- 设置微件使用的默认时区。可以通过手动选择时区静态设置，动态使用变量，或设为本地，使用只读者的本地时区。