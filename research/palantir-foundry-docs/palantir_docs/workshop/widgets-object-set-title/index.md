来源: https://palantir.com/docs/zh/foundry/workshop/widgets-object-set-title/

# 对象集标题

注意：以下翻译的准确性尚未经过验证。这是使用AIP ↗从原始英文文本进行的机器翻译。

# 对象集标题

对象集标题微件以标题的形式显示给定对象集的摘要。

## 配置选项

- 输入对象集决定将在微件中显示的对象数据的输入变量。
- 决定将在微件中显示的对象数据的输入变量。
- 包含单个Object如果开启，微件将显示输入对象集中的单个Object的标题。如果关闭，微件将显示对象类型的标题以及输入对象集中的Object总数。
- 如果开启，微件将显示输入对象集中的单个Object的标题。如果关闭，微件将显示对象类型的标题以及输入对象集中的Object总数。
- 显示图标如果开启，微件将显示输入对象集中的对象类型的图标。
- 如果开启，微件将显示输入对象集中的对象类型的图标。
- 启用拖拽允许将对象集中的对象拖到接受的放置区。必须安装数据银行服务，并且对象集中的对象数量少于500。
- 允许将对象集中的对象拖到接受的放置区。必须安装数据银行服务，并且对象集中的对象数量少于500。
- 标题覆盖允许使用选择的文本覆盖微件上显示的标题。
- 允许使用选择的文本覆盖微件上显示的标题。
- 在对象集为空时渲染微件是：允许选择一个对象类型作为占位符显示，如果输入的对象集为空。否：默认选项。如果输入的对象集为空，微件将不会在模块视图中渲染。
- 是：允许选择一个对象类型作为占位符显示，如果输入的对象集为空。
- 否：默认选项。如果输入的对象集为空，微件将不会在模块视图中渲染。