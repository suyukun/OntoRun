来源: https://palantir.com/docs/zh/foundry/workshop/widgets-property-list/

# 属性列表

注意：以下翻译的准确性尚未经过验证。这是使用AIP ↗从原始英文文本进行的机器翻译。

# 属性列表

属性列表微件显示来自单个提供的Object的属性列表。

## 配置选项

- 输入对象集决定将在微件中显示的对象数据的输入变量。如果对象集中包含多个Object，微件中只会显示第一个Object。
- 决定将在微件中显示的对象数据的输入变量。
- 如果对象集中包含多个Object，微件中只会显示第一个Object。
- 从场景加载数据如果启用，允许从选定的场景中加载Object数据。
- 如果启用，允许从选定的场景中加载Object数据。
- 设计调整微件中显示的属性位置。属性值可以显示在对应的属性类型标签旁边或下方。
- 调整微件中显示的属性位置。属性值可以显示在对应的属性类型标签旁边或下方。
- 属性配置选择将在微件中显示的属性，并指定显示的列数。
- 选择将在微件中显示的属性，并指定显示的列数。
- 隐藏空属性如果启用，空属性将从列表中隐藏。
- 如果启用，空属性将从列表中隐藏。