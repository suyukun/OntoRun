# OntoRun 后端依赖（安装：python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple）
# 版本与本地已验证环境对齐（/opt/anaconda3 python 3.12.7）：CI 与本地行为一致。
pydantic==2.13.2
openai==2.32.0  # A1 LLM provider（DeepSeek OpenAI 兼容 SDK）
pytest==9.0.3
pytest-cov==7.1.0
pytest-asyncio==1.3.0  # 与 pytest 9 兼容（本机 9.0.3 + 1.3.0 验证；预防性，仓库暂未 import）
ruff==0.16.3
fastapi==0.136.0
uvicorn==0.44.0
httpx==0.28.1  # fastapi TestClient（约 10 个测试文件模块级 import，缺则收集失败）
duckdb==1.5.5  # src/des/{materialize,metrics_materialize,contract}.py + 测试
PyYAML==6.0.3  # src/{config,metrics,calibrate}.py + 4 个测试文件 import yaml
